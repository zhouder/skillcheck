name: CI

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test:
    strategy:
      fail-fast: false
      matrix:
        node: [20, 22, 24]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v6
        with:
          node-version: ${{ matrix.node }}
          cache: npm
      - run: npm ci
      - run: npm run check

  package:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npm run test:coverage
      - run: npm run build:all
      - run: node dist/cli.js examples/csv-review
      - run: npm run package:check
      - run: node dist/cli.js --version

  action-smoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npm run build:all
      - name: Run the bundled Action against a valid skill
        shell: bash
        env:
          INPUT_PATH: examples/csv-review
          INPUT_FAIL-ON: warning
          INPUT_SARIF-FILE: .tmp/skillcheck.sarif
          GITHUB_OUTPUT: .tmp/valid-output
          GITHUB_STEP_SUMMARY: .tmp/valid-summary
        run: |
          mkdir -p .tmp
          : > "$GITHUB_OUTPUT"
          : > "$GITHUB_STEP_SUMMARY"
          node dist/action/index.js
          test -s "$GITHUB_OUTPUT"
          test -s "$GITHUB_STEP_SUMMARY"
          test -s .tmp/skillcheck.sarif
          grep -Eq '^score(=|<<)' "$GITHUB_OUTPUT"
          grep -Eq '^skills(=|<<)' "$GITHUB_OUTPUT"
          grep -Eq '^errors(=|<<)' "$GITHUB_OUTPUT"
          grep -Eq '^warnings(=|<<)' "$GITHUB_OUTPUT"
          grep -q '100' "$GITHUB_OUTPUT"
      - id: invalid
        name: Run the bundled Action against an invalid skill
        continue-on-error: true
        shell: bash
        env:
          INPUT_PATH: tests/fixtures/action-invalid
          INPUT_FAIL-ON: error
          INPUT_SARIF-FILE: ""
          GITHUB_OUTPUT: .tmp/invalid-output
          GITHUB_STEP_SUMMARY: .tmp/invalid-summary
        run: |
          : > "$GITHUB_OUTPUT"
          : > "$GITHUB_STEP_SUMMARY"
          node dist/action/index.js
      - name: Verify findings fail the Action
        shell: bash
        env:
          OUTCOME: ${{ steps.invalid.outcome }}
        run: test "$OUTCOME" = "failure"
